#include "main.h"
/**
 * _isdigit - check if c is a digit from '0' to '9'
 *
 * @c - the digit to check
 *
 * return: 1 if is a digit from '0' to '9', 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
